@{ 
    ViewBag.Title = "Course";
    Layout = "~/Views/Shared/AdminMaster.cshtml";
}
@using System.Data.Sql;
@using System.Data;
@using mycollage.Models;
<link href="~/Content/css/akash.css" rel="stylesheet" />
<script src="~/Scripts/jquery-1.8.2.min.js"></script>
<script src="~/Content/js/jquery.js"></script>
<script src="~/Content/js/course.js"></script>
<script src="~/Scripts/jquery-1.8.2.min.js"></script>
<script src="~/Content/css/akash.js"></script>
  <script>
    $(document).ready(function () {
        $("#example").DataTable();
    })

</script>

@*<table>
    @{
        {
        DataTable aa=db.GetAllRecord(ViewBag.sh);
        if (aa.Rows.Count > 0)
        { <tr><th>@aa.Rows[1]["branch"],,@aa.Rows[1]["year"],,@aa.Rows[1]["semester"]</th></tr>
            for(int i=0;i<aa.Rows.Count;i++)
            { 
    <tr><td>@aa.Rows[i]["course"]</td></tr>
    }
    }
    }
</table>*@
@*<div class="container-fluid" style="padding:0px;">
    <div class="col-sm-12" style="background:gray;min-height:50px; color:whitesmoke;font-weight:bolder;margin-bottom:2%;"><h4>Course Management</h4></div>
    <div class="col-sm-2"></div>
    <div class="col-sm-8" style="background:;min-height:50px; color:whitesmoke;font-weight:bolder;">
        <form action="/Admin/Course" method="post">
            <div class="row" style="font-size:20px;">
                
             
                <div class="col-sm-12" style="margin-top:5px;">
                    <select name="Branch" id="ddlbranch" class="col-sm-12 form-control">
                        <option value="australia" style="color:gray;">Brach</option>
                        <option value="CS">Computer Science</option>
                        <option value="EE">Electrical Enggr</option>
                        <option value="ELX">Electrnic Enggr</option>

                    </select>
                </div>
                <div class="col-sm-12" style="margin-top:5px;">
                    <select name="year" id="ddlyear" class="col-sm-12 form-control">
                        <option value="australia" style="color:gray;">Year</option>
                        <option>1</option>
                        <option>2</option>
                        <option>3</option>

                    </select>
                </div>
                <br />
                <div class="col-sm-12" style="margin-top:10px;">
                    
                    <input type="text"class="form-control" id="txtroll" name="Rollno" placeholder="Roll No"/>
                </div>
       
            </div>
            <div class="row" style="margin-top:10px;">
                <div class="col-sm-6 text-center">
                </div>
                <div class="col-sm-3 text-center">
                    <input type="submit" value="Submit" name="login" class="btn btn-success btn btn-block" style="background-color:#0c79c3; margin:0px;box-shadow:inset 0 0 2em black;" />
                </div>

              
            </div>
        </form>
    </div>
    <div class="col-sm-2"></div>
    <!--code for show details-->
    @{
        string cmd = "select * from AddCourse";
        //DatabaseManager db = new DatabaseManager();
        DataTable dt = db.GetAllRecord(cmd);
        if (dt.Rows.Count > 0)
        {
            <table class="table table-responsive" width="100%" style="display:none">
                <thead><tr style="background-color: #0c79c3; box-shadow: inset 0 0 2em black; border: 1px solid black; margin-top: 1%;color:white;font-size:18px; "><th>year</th><th>branch</th><th>semester</th><th>course</th></tr></thead>
                <tbody>

                    @for (int i = 0; i < dt.Rows.Count; i++)
                    {
                        <tr>
                            <td class="td1">@dt.Rows[i]["year"]</td>
                      
                            <td>@dt.Rows[i]["branch"]</td>
                            <td>@dt.Rows[i]["semester"]</td>
                            <td>@dt.Rows[i]["course"]</td>
                            
                        </tr>
                    }
                </tbody>
            </table>
        }
    }

    <!--End code for show details-->
</div>*@
<div class="col-sm-4" style="margin-top:4%">
    <form action="/Admin/Course" method="post">
        @Html.Raw(ViewBag.get)
        <input type="hidden" name="txtroll" value="@ViewBag.roll"/>
        <input type="submit" value="Allot subject" class="btn-success"/>
    </form>
</div>
<script src="~/Content/js/course.js"></script>
<!-------------------------asign subject---------------------------------------------->
 <!--code for show details-->

@*@using mycollage.Models;
@using System.Data;
<script src="~/Content/js/course.js"></script>
<script src="~/Content/css/akash.js"></script>*@

<div class="container-fluid">
    <div class="row text-center" style="min-height:50px;font-size:30px;color:black;margin-top:-1%;font-family:bolder;">
        <p style="margin-top:10px;">My_Subject</p>
    </div>
    <!--code for show details-->
    @{
        string cmd = "select * from Students";
        DatabaseManager db = new DatabaseManager();
        DataTable dt = db.GetAllRecord(cmd);
        if (dt.Rows.Count > 0)
        {
             <div class="col-sm-12" style="overflow-y:scroll;height:500px">
                 <table class="table table-responsive" id="example" width="100%" style="font-family:'Times New Roman'">
                     <thead><tr style="background-color: #0c79c3; box-shadow: inset 0 0 2em black; border: 1px solid black; margin-top: 1%;color:white;font-size:18px; "><th>Year</th><th>Branch</th><th>First Name</th><th>Last Name</th><th>Subject</th><th>Semester</th></tr></thead>
                     <tbody>

                         @for (int i = 0; i < dt.Rows.Count; i++)
                         {
                             <tr>


                                 <td class="td1" id="name">@dt.Rows[i]["year"]</td>
                                 <td>@dt.Rows[i]["Branch"]</td>
                                 <td>@dt.Rows[i]["FName"]</td>
                                 <td>@dt.Rows[i]["LName"]</td>
                                 <td>@dt.Rows[i]["subject"]</td>
                                 <td>@dt.Rows[i]["semester"]</td>

                             </tr>
                         }
                     </tbody>
                 </table>
                 </div>
        }
    }

    <!--End code for show details-->
</div>
